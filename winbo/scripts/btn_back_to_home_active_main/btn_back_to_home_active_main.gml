/// @function btn_back_to_home_active_main
/// @summary Navigate back to the home tab when button is pressed.
/// @returns {void}
function btn_back_to_home_active_main() {
	tab_swap(o_ui.menu_tab[MenuTab.home]);
}
