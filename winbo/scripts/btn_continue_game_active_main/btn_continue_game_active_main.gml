/// @function btn_continue_game_active_main
/// @summary Continue a saved game session when button is pressed.
/// @returns {void}
function btn_continue_game_active_main() {
	game_state_set_target(GameState.start);
}
