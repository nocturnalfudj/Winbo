/// @function btn_new_game_active_main
/// @summary Start a new game session when button is pressed.
/// @returns {void}
function btn_new_game_active_main() {
	game_state_set_target(GameState.start);
}
