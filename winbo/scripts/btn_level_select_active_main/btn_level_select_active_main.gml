/// @function btn_level_select_active_main
/// @summary Navigate to the level select tab when button is pressed.
/// @returns {void}
function btn_level_select_active_main() {
	tab_swap(o_ui.menu_tab[MenuTab.level_select]);
}
