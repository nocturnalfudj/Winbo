/// @function btn_options_active_main
/// @summary Navigate to the settings tab when button is pressed.
/// @returns {void}
function btn_options_active_main() {
	tab_swap(o_ui.menu_tab[MenuTab.settings]);
}
